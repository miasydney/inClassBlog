<!DOCTYPE html>
<html>
  <head>
    <title>InClassBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <%= render "shared/nav" %>
  
  <%# alerts from devise gem : will display under nav bar but above body of page %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

    <%= yield %>
    <%# following lines means i want to only have the console there in the development environment  %>
    <%# (not the deployment environment) %>
    <% if Rails.env.development? %>
    <% console %>
    <% end %>
  </body>
</html>
